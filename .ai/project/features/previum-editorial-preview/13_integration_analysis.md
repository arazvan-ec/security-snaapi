# Integration Analysis: previum-editorial-preview

## Summary

- Entities: 0 extended, 0 modified, 2 new (PreviumUser, HttpTokenValidator)
- Endpoints: 0 extended, 0 modified, 1 new (`GET /previum/{id}`)
- Business Rules: 0 conflicts, 3 new
- Status: **CLEAR**

---

## Entities Impact

### New

| Entity/Service | Purpose | Relationships |
|----------------|---------|---------------|
| `PreviumUser` | Authenticated previum user (Symfony UserInterface) | Standalone — no persistence |
| `HttpTokenValidator` | JWT validation via external HTTP microservice | Uses HTTPlug client |

### Extended / Modified
_None_ — No existing entities change.

---

## API Contracts Impact

### New

| Endpoint | Method | Auth Required |
|----------|--------|---------------|
| `/previum/{id}` | GET | YES — Bearer JWT |

**Contract**:
```
GET /previum/{editorialId}
Headers: Authorization: Bearer <jwt_token>
200: Editorial data (same format as /editorials/{id})
401: {"errors": ["Unauthorized"]}
403: {"errors": ["Forbidden: insufficient permissions"]}
404: {"errors": ["Editorial not published"]}
```

### Existing (unchanged)
| Endpoint | Change | Backward Compatible |
|----------|--------|---------------------|
| `GET /editorials/{id}` | **NO CHANGE** | N/A |

---

## Business Rules

### Conflicts
_None._

### New

| Rule ID | Description |
|---------|-------------|
| BR-PREV-01 | All `/previum/*` requests require valid Bearer JWT |
| BR-PREV-02 | Only `user_type=previum` in validated JWT grants access |
| BR-PREV-03 | Previum users see editorials regardless of `isVisible()` status |

---

## Compatibility

- **Backward Compatible**: YES
- **Migration Required**: NO
- **Breaking Changes**: NONE

---

**Generated by**: workflows:plan (Phase 2 - Integration Analysis)
**Date**: 2026-02-09
