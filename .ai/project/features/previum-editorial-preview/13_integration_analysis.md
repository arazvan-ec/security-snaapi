# Integration Analysis: previum-editorial-preview

## Summary

- Entities: 0 extended, 0 modified, 2 new (PreviumUser, PreviumTokenValidator)
- Endpoints: 0 extended, 0 modified, 1 new (`GET /previum/{id}`)
- Business Rules: 0 conflicts, 3 new
- Status: **CLEAR** (no conflicts with existing architecture)

---

## Entities Impact

### Extended
_None_ — No existing entities are modified.

### Modified
_None_ — No existing entities change behavior.

### New

| Entity/Service | Purpose | Relationships |
|----------------|---------|---------------|
| `PreviumUser` | Represents an authenticated previum user (Symfony UserInterface) | Standalone — no persistence |
| `PreviumTokenValidator` | Encapsulates JWT validation via external HTTP microservice | Uses HTTP Client |

---

## API Contracts Impact

### Extended
_None_ — No existing endpoints gain new parameters.

### Modified
_None_ — No existing endpoints change behavior.

| Endpoint | Change | Backward Compatible |
|----------|--------|---------------------|
| `GET /editorials/{id}` | **NO CHANGE** | N/A |

### New

| Endpoint | Method | Purpose | Auth Required |
|----------|--------|---------|---------------|
| `/previum/{id}` | GET | Serve editorial content (including unpublished) for previum users | YES — Bearer JWT |

#### `/previum/{id}` Contract

```
GET /previum/{editorialId}
Headers:
  Authorization: Bearer <jwt_token>

Responses:
  200: Editorial data (same format as /editorials/{id})
  401: {"errors": ["Unauthorized"]}
  403: {"errors": ["Forbidden: insufficient permissions"]}
  404: {"errors": ["Editorial not published"]}
```

---

## Business Rules Impact

### Conflicts
_None detected._

The new endpoint operates on a completely separate route with its own firewall. The existing `GET /editorials/{id}` endpoint is untouched.

### New

| Rule ID | Scope | Description |
|---------|-------|-------------|
| BR-PREV-01 | Authentication | All requests to `/previum/*` must include a valid Bearer JWT token |
| BR-PREV-02 | Authorization | Only users with `user_type=previum` in the validated response JWT can access content |
| BR-PREV-03 | Content Access | Previum users can view editorials regardless of `isVisible()` status |

---

## Compatibility Assessment

- **Backward Compatible**: YES — No existing endpoint, service, or configuration is modified
- **Migration Required**: NO — New files only
- **Breaking Changes**: NONE
- **Impact on Existing Tests**: NONE — All existing tests pass without modification

---

## Infrastructure Integration

### HTTP Client (httplug)
- **Pattern exists**: `config/packages/httplug.yaml` already defines `app_guzzle7` client with retry plugin
- **Integration**: Add a new `previum_auth` client definition following the same pattern
- **No conflict**: New client is independent, does not affect existing clients

### Symfony Security
- **New dependency**: `symfony/security-bundle` must be added (not currently installed)
- **Firewall isolation**: New `previum` firewall only affects `/previum/*` routes
- **Main firewall**: Explicitly set to `security: false` to preserve current open behavior

### Environment Variables
- **Pattern exists**: `.env.dist` follows `###> section ###` / `###< section ###` convention
- **Integration**: Add new section for previum auth configuration
- **No conflict**: New variables are independent

---

**Generated by**: workflows:plan (Phase 2 - Integration Analysis)
**Date**: 2026-02-09
